# Agentic RAG stack — pin versions in production after validation
# Python >= 3.10 recommended

# Core LLM / embedding (local models)
torch>=2.1.0
transformers>=4.51.0
sentence-transformers>=3.0.0
accelerate>=0.26.0

# vLLM: install separately to match your CUDA/driver (see SETUP.md)
# pip install vllm

# LangChain / LangGraph
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-qdrant>=0.2.0
langchain-text-splitters>=0.3.0
langgraph>=0.2.0

# Qdrant client (also used by langchain-qdrant)
qdrant-client>=1.12.0

# HTTP / utils
httpx>=0.27.0
pydantic>=2.0.0
python-dotenv>=1.0.0
