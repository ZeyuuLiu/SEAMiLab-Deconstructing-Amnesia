# TiMem Dependencies - Updated for Python 3.10 compatibility
# ============================================================================
# Core AI/ML Framework
# ============================================================================
# accelerate>=1.10.0  # Only needed for local model training
# torch>=2.0.0  # Only needed for local models
# transformers>=4.50.0  # Only needed for local models
# sentence-transformers>=5.0.0  # Only needed for local SentenceBERT embedding
huggingface-hub>=0.33.0
# safetensors>=0.4.0  # Only needed for local models
tiktoken>=0.9.0

# ============================================================================
# LangChain & LangGraph
# ============================================================================
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.3.0
langchain-anthropic>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
langgraph>=0.6.0
langgraph-checkpoint>=2.0.0
langgraph-prebuilt>=0.6.0
langgraph-sdk>=0.2.0
langsmith>=0.4.0

# ============================================================================
# LLM Providers
# ============================================================================
openai>=1.100.0
anthropic>=0.69.0
zhipuai>=2.1.0

# ============================================================================
# Web Framework
# ============================================================================
fastapi>=0.115.0
uvicorn>=0.30.0
starlette>=0.40.0
gradio>=4.40.0
gradio-client>=2.0.0

# ============================================================================
# Database - PostgreSQL
# ============================================================================
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
SQLAlchemy>=2.0.0

# ============================================================================
# Database - Vector (Qdrant)
# ============================================================================
qdrant-client>=1.15.0

# ============================================================================
# Async Utilities
# ============================================================================
aiohttp>=3.10.0
aiofiles>=23.0.0
aiosignal>=1.4.0
aiosqlite>=0.20.0
aiomysql>=0.2.0

# ============================================================================
# Data Processing
# ============================================================================
numpy>=2.1.0
pandas>=2.2.0
scipy>=1.15.0
scikit-learn>=1.5.0

# ============================================================================
# NLP
# ============================================================================
nltk>=3.9.0
jieba>=0.42.0
regex>=2024.0.0

# ============================================================================
# Configuration & Validation
# ============================================================================
pydantic>=2.11.0
pydantic-settings>=2.10.0
pydantic-core>=2.33.0
python-dotenv>=1.1.0
PyYAML>=6.0.0

# ============================================================================
# HTTP & Networking
# ============================================================================
httpx>=0.27.0
httpcore>=1.0.0
requests>=2.32.0
urllib3>=2.5.0

# ============================================================================
# Utilities
# ============================================================================
click>=8.2.0
typer>=0.17.0
rich>=14.0.0
tqdm>=4.67.0
python-dateutil>=2.9.0
python-multipart>=0.0.20
colorama>=0.4.0

# ============================================================================
# Testing
# ============================================================================
pytest>=8.4.0
pytest-asyncio>=1.1.0

# ============================================================================
# Task Scheduling
# ============================================================================
APScheduler>=3.11.0

# ============================================================================
# Logging
# ============================================================================
structlog>=25.0.0

# ============================================================================
# Evaluation & Metrics
# ============================================================================
rouge>=1.0.0

# ============================================================================
# Data Serialization
# ============================================================================
orjson>=3.10.0
ormsgpack>=1.10.0
protobuf>=6.0.0

# ============================================================================
# Code Quality
# ============================================================================
ruff>=0.14.0

# ============================================================================
# Other Utilities
# ============================================================================
# The following packages are transitive dependencies automatically installed
# by pip and should not be explicitly listed in requirements.txt:
# anyio, attrs, certifi, charset-normalizer, distro, filelock, frozenlist,
# fsspec, idna, importlib-resources, iniconfig, Jinja2, joblib, jsonpatch,
# jsonpointer, jsonschema, markdown-it-py, MarkupSafe, mdurl, multidict,
# mypy-extensions, networkx, packaging, pillow, pluggy, pyasn1, Pygments,
# PyJWT, pyparsing, pytz, sniffio, tenacity, threadpoolctl, tokenizers,
# tomli, typing-extensions, tzdata, tzlocal, watchfiles, websockets,
# xxhash, yarl, zstandard

# ============================================================================
# Windows-specific
# ============================================================================
pywin32>=306; sys_platform == 'win32'
win32-setctime>=1.2.0; sys_platform == 'win32'

# ============================================================================
# Optional: Evaluation dependencies
# ============================================================================
# bert-score>=0.3.0  # Requires numpy<2.0, may cause conflicts with Python 3.13
# bitsandbytes>=0.47.0  # Requires CUDA, Linux only
